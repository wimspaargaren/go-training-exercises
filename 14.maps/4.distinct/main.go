package main

import (
	"fmt"
)

func main() {
	fmt.Println("Distinct elements: ", DistinctElements([]string{"1", "2", "2", "3", "4", "4"}))
}

// DistinctElements Implement the DistinctElements function which returns
// the number of distinct elements in a given string slice.
// For example, for the input ["1", "2", "2", "3", "4", "4"], the result should be 4,
// because there are 4 different values: "1", "2", "3", and "4".
// Use a map to get the result.
// Run the tests to verify your implementation.
func DistinctElements(list []string) int {
	return -1
}
